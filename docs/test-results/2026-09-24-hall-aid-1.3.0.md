Sovereign 1.3.0 hallway pairs and aid recovery
============================================

Scope and evidence
------------------

The author authorized the combined blessing fix, inactive fast path, hallway
lit/unlit pairs, retained diagnostics and thirty-second room ignition for deployment.
Before edits, all affected saved editor inputs matched the repo; an unrelated
Hewg dialogue-source difference was left untouched. Backups and native evidence
are in `.codex-temp/hall-aid-1.3.0/`; the preceding timing-only backup is in
`.codex-temp/room-ignition-30s/`.

HKS now initializes the call counter to zero, initial tier to -1 and pending
adjustment to false. Missing globals previously resolved to dummy functions and
threw in Update. After one recovery scan, inactive aid reads arena permission and
returns without resource reads or timer work. Active withdrawal remains ten
seconds and does not reduce living HP below one. Diagnostics retain their bounds
and now identify version 1.3.0. No other movement mechanisms were changed.

Hallway events 5750320-5750331 retain their region/timer/progression conditions.
They switch 125 lit originals against unlit duplicates 18006000-18006124.
Automatic effects use offset 50; empty copies use offset 61. Four missing family
rows are added, with all existing rows preserved. Source simulation checks all
12 regions, first-group delayed cancellation, exit/reentry, victory/crystal and
saved-state loads, asserting one enabled member per pair. This is not a rendering
test; no in-game one-group acceptance was available during implementation.

Verification
------------

- Native map and regulation roundtrips passed. Removing the intended duplicate
  placements and restoring original offsets reproduces the complete original map
  snapshot. Other parameter tables, old rows and binder metadata are unchanged.
- Compiled event differences are only the twelve hallway events, relative to the
  already accepted thirty-second event binary. All other runtime event candidates
  are equivalent. The earlier timing pass verified 95 changed delays and one
  sixty-to-thirty completion timer, preserving the first 0.05-second start.
- Lighting simulation passed room ignition, brightness, rescue/death/retry,
  victory/crystal and hallway pair states.
- Five Lua-source tests passed across aid, diagnostics and beginner rescue.
  Aid/diagnostic mocks now include the actual missing-global fallback; inactive
  reads, recovered tiers, proportional resources, delayed maxima and safe
  withdrawal are covered. These tests do not qualify real HKS engine behavior.
- Coordinated player qualification reused unchanged native asset evidence with
  fresh HKS guards. Four guarded handoffs synced five changed editor files.
- Package review found exactly four changed runtime files versus 1.2.9; all
  remaining 69 files, including the external DLL dependency, are preserved.

Manual acceptance
-----------------

ER-113/114/115 and the existing aid-resource tests remain pending in game. Verify
red/unlit rendering in the first hallway and all other groups, room ignition over
thirty seconds, aura/stat gain, safe withdrawal and completed Update checkpoints.
The confirmed aid error is fixed in source; this does not establish that the
original intermittent movement stop is resolved. Fully restart the game for the
next test and retain both diagnostic logs.

Deployment receipts
-------------------

Prepared package: `.vdb/prepared/34f54e09840f4e86af345523870ad1c0/receipt.json`.
Protocol-3 finalization: `.vdb/finalizations/hall-aid-1.3.0/receipt.json`.
See the finalization receipt for deployment state and live verification.

Finalization completed for enabled profile SkC-QjDMc. Build
666993d638a87355918c746e is selected; staged and live verification report no
differences. The texture selection is unchanged. Deployment is verified, while
gameplay acceptance remains pending.
