# Rick audio/recovery and movement isolation: 1.2.1

The user reports improved transition behavior on 1.2.0, missing Hoarah/Wrath sound,
and persistent movement locking despite the disabled shared animation-control
helper. A fog entrance was one observed context; it is not an established cause.
The prior 1.2.0 finalization was refreshed and confirmed deployed/verified before
selecting it as this batch's baseline.

Implemented a half-second protected recovery before Rick resumes AI. Local audio
now requests native category-5 casting cue 525305 and explosion cue 525316 at the
warning and shot gates, respectively. The Hoarah vocal 472108006 uses its native
loaded carrier 18000852, warped to the soldier before playback. Carrier AI,
rendering, collision and gravity remain disabled; no bank or map was changed.
Static backread and native bank/TAE evidence do not prove in-game audibility.

Restore ModAnimationControl and bypass only the ModJump update call. This disables
custom airborne actions and the plunge-damage updater contained in that function,
while ordinary jumping/landing and other cancellation paths remain intact. The
movement cause is still unconfirmed; restart and retest fog entry and ground movement.

Fourteen mocked control-flow checks pass, including recovery death cleanup and
one-time audio gate ordering/cancelation. Compilation and decoded preservation
confirm only events 18002860/18002861 changed. Comparison against the backup
confirms only the intended player call switch/comments changed. Player native
qualification was reused with updated HKS guards. Preparation/version checks pass.
Git's broader working-tree whitespace check reports an existing mixed-indent line
736 in c0000; the backup has the identical line. The current-change comparison
against that backup passes whitespace checking; unrelated indentation is preserved.

Editor baselines matched before edits. Qualified handoffs synchronized the player
script and event source/binary. The full package differs from 1.2.0 only in c0000.hks
and the Graveyard event binary. No gameplay pass is inferred from static checks.

Backups: `.codex-temp/rick-1.2.1/`.
Prepared receipt: `.vdb/prepared/eea3676432bc455e9f30f5e1186534a5/receipt.json`.
Finalization: `.vdb/finalizations/rick-1.2.1/receipt.json`.

Deployment completed for Default profile SkC-QjDMc. Build
`e2b2072026f6ecde80ed682c` was selected after Vortex verified enabled/deployed
bytes with no differences. Both changed live files were independently hashed
against the prepared package. Audio, recovery feel and movement diagnosis still
require in-game testing.
