# Rick stagger-state correction: 1.2.0

Implemented the authored W_SABreak entry and charge-only state hold using the
existing encounter markers. New neutral SpEffect 1627119 acknowledges activation;
the event waits at most one second, then gives the full two-second warning. The
incoming Rick has no second kneel request or three-second AI-disabled recovery.
His AI is enabled and replanned at the shot; the independent vocal continues.
Death, abort and retry clear the charge and acknowledgement.

Eleven event control-flow tests pass, covering admission/loading, critical wait,
death boundaries, bounded acknowledgement fallback, immediate AI handoff, retries,
music and the unchanged room-lighting simulation. These are mocked source tests,
not engine animation or targeting tests. Native compilation and decoded comparison
confirm only event 18002860 changed. Only three HKS functions differ from the
pre-edit backup; other enemy and all player code are preserved. Regulation
roundtrip verifies one added neutral marker and no changes to existing rows or
other tables. Repository preparation/version/whitespace checks pass.

Saved editor baselines matched before editing. Qualified handoffs synchronized
Script's c9997 HKS and map event source/binary, and Smithbox regulation. The full
main package differs from 1.1.9 in only those three runtime files. No graph, TAE,
map, lighting, Wrath tuning or reward changes are included. In-game pose hold,
contact without criticals, death/retry and immediate pursuit remain pending.

Backups and preservation checks: `.codex-temp/rick-1.2.0/`.
Prepared: `.vdb/prepared/071cf0a47dd7438f92fedca5150ad5e5/receipt.json`.
Finalization: `.vdb/finalizations/rick-1.2.0/receipt.json`.
Staging completed as build `844748d58d7812ef2e8612b9`. Deployment is still queued:
Vortex reports "Activate game 'eldenring' in Vortex first" for Default profile
`SkC-QjDMc`. Request `14ca6c5b-fe59-4dfe-8a00-68a78d0e1020` remains active and
will resume when that profile becomes available. No live-byte verification or
packaging-selection promotion has been claimed. Resume this receipt; do not
submit another build/request.
