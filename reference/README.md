Unassigned map reference copies
==============================

These two files were retained from the former root `MapStudio` folder during the
layout migration. They are not package inputs or active authoring sources. Their
original game/mod version has not been established; do not choose them by timestamp
or propagate them over accepted runtime files.

| File | Comparison with accepted runtime at migration |
| --- | --- |
| `mapstudio/m18_00_00_00.msb.dcx` | Different; retained for reference without assigning a version |
| `mapstudio/m60_43_50_00.msb.dcx` | Byte-identical duplicate |

Active maps are under `mod/map/`; the authoring workspace is the configured Smithbox
directory. The independent backups and old/new path map are recorded in the
[workflow checkpoint](../docs/WORKFLOW-PREP-CHECKPOINT.md).
