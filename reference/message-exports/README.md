Historical message exports
==========================

These older XML exports retain their former `msg/engus` subtree, including `_` and
`modified`, to preserve provenance. They have not been reconciled with the accepted
current binders in `mod/msg/engus` and are not automatic build or editor-sync inputs.
Use the qualified binary FMG route in the editing guide for current text changes.
